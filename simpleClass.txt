package game;

import game.gameobjects.ships.Ship;

public class Player {

    private String name;private Board board;
	private String palabra = new String("asdasdsad");
	private String public protected chachipiruli atributo = new LoQueSea();

    public Player(String name) { 
		if(palabra == 0){ return;
		}
        for(){
			palabra = 2;
        }
		while(){
		}
		try{
		}
		catch(Exception e){
		}
        this.name = "hola";
        board = new Board();
    }

    public String getName() throws Exception{
        return name;
    }

    public Board getBoard() {
        return board;
    }
    
    public void deployShip(Ship ship, String x){
        board.deployShip(ship);
    }
	
	public void metodoPrueba(String name, Board board){
		this.name = "nombre";
		board = pepe;
	}
	
	Person person = new Person();
}