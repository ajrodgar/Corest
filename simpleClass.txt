package game;

import game.gameobjects.ships.Ship;

public class Player {
//Esto es un comentario que no debe salir 
/*Esto es
un bloque
de comentario*/
@Override
	private String name;private Board board; //Esto es otro comentario que tampoco tiene que salir
	private String palabra = new String("asdasdsad");
	private String palabra2 = new String("asdasdsad");
	private String public protected chachipiruli atributo = new LoQueSea();
	private int[] array ={1,2,3};

    public Player(String name) { 
		if(palabra == 0){ return;
		}
        for(){
			palabra = 2;
        }
		while(){
    	}
		try{
		}
		catch(Exception e){
		}
        this.name = "hola";
        board = new Board();
    }

    public String getName() throws Exception{/*Esto es
un bloque
de comentario*/
		return name;
    }

    public Board getBoard() {
        return board;
    }
    
    public void deployShip(Ship ship, String x){
        board.deployShip(ship);
    }
	
	public void metodoPrueba(String name, Board board){
		this.name = "nombre";
		board = pepe;
	}
	
	Person person = new Person();
}